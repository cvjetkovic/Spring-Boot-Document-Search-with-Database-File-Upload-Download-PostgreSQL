## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
#Vlada baza:

#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
#spring.datasource.username= postgres
#spring.datasource.password=admin

#ES baza dvdrental:

spring.datasource.url=jdbc:postgresql://192.168.67.245:5432/dvdrental
spring.datasource.username= postgres
spring.datasource.password= Manager1




## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

#FTS
native-query.package-scan=io.github.gasparbarancelli.demospringnativequery

#server.port=9090

#server.servlet.context-path=/aplikacija



# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

## Hibernate Logging
logging.level.org.hibernate.SQL= DEBUG


## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

